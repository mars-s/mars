# 线上地址参考。按需复制到 .env；部署覆盖也可放在未跟踪的 .env.local 中。
ZCODE_BASE_URL=https://zcode.z.ai
BIGMODEL_API_BASE_URL=https://bigmodel.cn
ZAI_OAUTH_ORIGIN=https://chat.z.ai
ZAI_BUSINESS_BASE_URL=https://api.z.ai
# 线上公开 OAuth client id，不是 secret。
ZAI_OAUTH_CLIENT_ID=client_P8X5CMWmlaRO9gyO-KSqtg

# 没有内置的资源源站。留空即表示"未配置远程资源源站"，应用不会访问任何第三方主机：
# 开发环境走本地 mock CDN，打包构建会显式报错而不是悄悄连上别人的服务器。
# 需要远程资源（官方市场目录、商店图标、版本发布包）时，把下面三项指向你自己的自建源站。
# 远程发布资源 base URL，例如 https://your-host/zcode/electron/releases
ZCODE_CDN_BASE_URL=
# 远程资源完整覆盖地址，必须包含与运行版本匹配的版本号；留空则按 ZCODE_CDN_BASE_URL 推导。
ZCODE_REMOTE_ASSET_CDN_BASE_URL=
# 官方插件市场目录地址（marketplace.json 的 URL，或 owner/repo 的 git 简写）。
# 留空时官方市场只保留本地内置插件，插件商店本身照常可用，个人来源不受影响。
ZCODE_OFFICIAL_MARKETPLACE_SOURCE=
# 官方商店图标资源 base URL；留空时列表回退到默认占位图。
ZCODE_OFFICIAL_PLUGIN_ASSETS_BASE_URL=

# 导入会话的来源回链 base，不改变 API；保留 /cn/share，不含分享码。
# 修改 API origin 时同步修改此值，或删除此项以自动跟随 API origin；不要赋空值。
ZCODE_CONVERSATION_SHARE_WEB_URL=https://zcode.z.ai/cn/share

# 可选本地 Provider 配置文件路径；留空使用内置线上配置。
ZCODE_BUILTIN_PROVIDER_CONFIG_FILE=

# 内网或私有部署地址没有线上默认值，按需显式填写。
ZCODE_DEPS_BASE_URL=
INTRANET_MACHINE_HOST=
ZCODE_DIST_BASE_URL=
