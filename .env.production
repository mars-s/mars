# No endpoint defaults ship with this fork, in production either. The shared
# endpoint resolvers return an empty string when nothing is configured, so a
# packaged build must be given ZCODE_BASE_URL (or ZCODE_ENDPOINT_ORIGIN) and the
# other origin variables at deploy time, usually via the untracked .env.local.
