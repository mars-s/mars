// Rough character-to-token ratio used wherever only a character count is available.
// Deliberately coarse: it feeds UI estimates and compaction pre-checks, never billing.
export const ESTIMATED_TOKEN_CHAR_DIVISOR = 3;
